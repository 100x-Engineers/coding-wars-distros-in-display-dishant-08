let completion = []; // Initialize the completion variable

function setCompletion(complete) {
  completions.push(completion);
}

function getCompletion() {
  return completion;
}

module.exports = {
  setCompletion,
  getCompletion,
};